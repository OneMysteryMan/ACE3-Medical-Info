"use strict";

const Errors = {
	DOWNLOAD: `download${Math.random()}`,
	PARSE: `parse${Math.random()}`,
	PARSE_CLASS: `parse_class${Math.random()}`,
	PARSE_VARIABLE: `parse_variable${Math.random()}`,
	RENDER: `render${Math.random()}`,
}

export default Errors;